CREATE DATABASE AIEnglishDB;
GO

USE AIEnglishDB;
GO

CREATE TABLE users (
    id INT IDENTITY(1,1) PRIMARY KEY,
    username NVARCHAR(50) UNIQUE NOT NULL,
    password NVARCHAR(255) NOT NULL,
    fullname NVARCHAR(100) NOT NULL
);
GO

CREATE TABLE history (
    id INT IDENTITY(1,1) PRIMARY KEY,
    user_id INT NOT NULL,
    english_word NVARCHAR(100) NOT NULL,
    vietnamese_meaning NVARCHAR(255) NOT NULL,
    confidence FLOAT NULL,
    detected_time DATETIME DEFAULT GETDATE(),

    CONSTRAINT FK_History_Users
        FOREIGN KEY(user_id)
        REFERENCES users(id)
);
GO

INSERT INTO users (username, password, fullname)
VALUES
('admin', '123456', N'Administrator'),
('student1', '123456', N'Nguyen Van A'),
('student2', '123456', N'Tran Thi B'),
('teacher1', '123456', N'Le Van C');
GO
